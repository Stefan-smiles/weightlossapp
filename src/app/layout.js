export const metadata = {
  title: "Weight Loss Tracking app",
  description: "Weight Loss Tracking app created by smiles",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
        {children}
    </html>
  );
}
